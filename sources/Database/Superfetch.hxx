#pragma once

#include <Database/PrefetchEngine.hxx>

namespace AstralDB {

/** Legacy name — use \c PrefetchEngine for row/join scan prefetch. */
using Superfetch = PrefetchEngine;

} // namespace AstralDB
